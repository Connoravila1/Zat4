# Build artifacts — regenerated by `zig build`, never committed.
.zig-cache/
zig-out/

# Logs and transient output.
*.log

# Reference screenshots (not source).
Screenshot*.png
file.pub

# Internal guiding documents — kept on disk for working context, never tracked
# (private infra/strategy). Public, user-facing docs are excepted below.
*.md
!README.md
!ZAL_LLM_REFERENCE.md
To_compile_zig
Zig_References
mockup_desktop.html
sample_events.jsonl

# local launcher (holds the AppView read token)
run-local.sh
chat-test.sh
chat-test-b.sh
transparency-preview.png
deploy/angle-win64/*.dll
deploy/android-debug.keystore
