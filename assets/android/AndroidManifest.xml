<?xml version="1.0" encoding="utf-8"?>
<!-- The whole app is native code: hasCode="false" means no dex at all —
     the framework's NativeActivity loads libzat.so (android.app.lib_name)
     and calls its ANativeActivity_onCreate (src/android_activity.zig). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zat4.client"
    android:versionCode="1"
    android:versionName="0.1.0">
  <uses-sdk android:minSdkVersion="29" android:targetSdkVersion="34"/>
  <uses-permission android:name="android.permission.INTERNET"/>
  <!-- debuggable: test builds only. It is what lets `adb run-as` provision
       the session file into the app's private files dir before on-device
       sign-in exists (M-And.5). The store build drops it. -->
  <application
      android:label="Zat4"
      android:icon="@mipmap/ic_launcher"
      android:hasCode="false"
      android:debuggable="true"
      android:extractNativeLibs="true">
    <activity
        android:name="android.app.NativeActivity"
        android:exported="true"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:windowLayoutInDisplayCutoutMode="shortEdges"
        android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|keyboard|uiMode">
      <meta-data android:name="android.app.lib_name" android:value="zat"/>
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <!-- The OAuth redirect (M-And.5): the browser hands
           com.zat4.pds:/callback?... back to the app as a VIEW intent; a
           trampoline instance ferries it to the armed login flow
           (android_activity.zig) and re-fronts the running task. The scheme
           is the client_id hostname reversed, per the atproto OAuth spec. -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="com.zat4.pds"/>
      </intent-filter>
    </activity>
  </application>
</manifest>
