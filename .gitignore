# Build artifacts — regenerated by `zig build`, never committed.
.zig-cache/
zig-out/
# Stray compiled binaries from manual `zig build-exe` at the repo root — build
# outputs, never source. `gpu_smoke` (an 11 MB harness binary) was committed by a
# blanket `git add` in c5552f3; keep root binaries out for good.
/gpu_smoke
/gpu_preview
/zat-gpu-smoke
/zat-gpu-preview

# Internal design mockups — standalone HTML prototypes kept on disk for working
# context, never tracked (same posture as the *.md guiding docs below). None are
# user-facing; if a real public HTML ever ships, except it explicitly.
*.html

# Logs and transient output.
*.log

# Reference screenshots (not source).
Screenshot*.png
file.pub

# Internal guiding documents — kept on disk for working context, never tracked
# (private infra/strategy). Public, user-facing docs are excepted below.
*.md
!README.md
!ZAL_LLM_REFERENCE.md
To_compile_zig
Zig_References
mockup_desktop.html
sample_events.jsonl

# local launcher (holds the AppView read token)
run-local.sh
chat-test.sh
chat-test-b.sh
transparency-preview.png
deploy/angle-win64/*.dll
deploy/android-debug.keystore
chat-live.sh
.relay-token
